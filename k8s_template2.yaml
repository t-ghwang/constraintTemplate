apiVersion: templates.gatekeeper.sh/v1beta1
kind: ConstraintTemplate
metadata:
  name: k8scompliant
spec:
  crd:
    spec:
      names:
        kind: K8sCompliant
      validation:
        # Schema for the `parameters` field
        openAPIV3Schema:
          type: object
          properties:
            message:
              type: string
            labels:
              type: array
              items:
                type: object
                properties:
                  key:
                    type: string
                  allowedRegex:
                    type: string
  targets:
    - target: admission.k8s.gatekeeper.sh
      code:
        - engine: K8sNativeValidation
          source:
           apiVersion: admissionregistration.k8s.io/v1alpha1
           kind: ValidatingAdmissionPolicy
           spec:
             paramKind:
               apiVersion: "foobar/v1"
               kind: "Placeholder"
             validations:
             - expression: "params.spec.parameters.labels.all(entry, has(object.metadata.name) && entry.key in object.metadata.name)"
               messageExpression: '"missing required name, requires all of: " + params.spec.parameters.labels.map(entry, entry.key).join(", ")'
             - expression: "!params.spec.parameters.labels.exists(entry, has(object.metadata.name) && entry.key in object.metadata.name && !string(object.metadata.name[entry.key]).matches(string(entry.allowedRegex)))"
               message: "regex mismatch"